This is a Python notebook project that uses Selenium.

Make sure you have a Jupyter notebook (or similar) installed.

Install selenium by
- pip install selenium

Open AddToGroup.ipynb to find more instructions.
